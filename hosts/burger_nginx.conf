upstream app {
    server backend;
}

server {
    listen *:80;
    listen [::]:80;
    location / {
        proxy_pass http://127.0.0.1:8000/;
    }

    location /media/ {
        alias /app/media/;
    }

    location /static/ {
        alias /app/collected_static/;
    }
}

