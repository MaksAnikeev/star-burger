upstream app {
    server backend:8000;
}

server {
    listen 80;
    location / {
        proxy_pass http://0.0.0.0:8000/;
    }

    location /media/ {
        alias /app/media/;
    }

    location /static/ {
        alias /app/collected_static/;
    }
}

